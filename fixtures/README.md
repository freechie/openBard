# Fixtures

`c-major-chord.wav` is a 2-second, 44.1 kHz mono synthesis of C4, E4, and G4
together. It is original project audio, not a commercial recording.

The matching note events live in `contracts/transcription.example.json`.
